Compass + GPS is a simple app for Android that shows a compass based on the device's compass sensor, plus geo-location (latitude and longitude) information based on device's GPS sensor (must be enabled).